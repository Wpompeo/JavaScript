const notas = [7, 7, 8, 9];

//Utilizasse as ... para executar a copia e incluir um novo valor.
const novasNotas = [...notas, 10];

console.log(`As novas notas são: ${novasNotas}`);
console.log(` As notas originais são: ${notas}`);